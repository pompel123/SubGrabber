# Updated SConstruct file for Flipper Zero FAP build configuration

# Import environment
env = Environment()

# Build the universal_grabber application
env.Program(target='universal_grabber', source='universal_grabber.c')
